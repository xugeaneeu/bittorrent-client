package dispatcher;

import bencode.torrent.TorrentMeta;
import network.NetworkReactor;
import storage.FileManager;

public class DownloadScheduler {
  public DownloadScheduler(TorrentMeta meta, FileManager fm, NetworkReactor reactor, PeerManager peerManager) {

  }
}
